networks=CREATE TABLE APP.NETWORKS (ID INTEGER NOT NULL GENERATED ALWAYS AS IDENTITY (START WITH 1, INCREMENT BY 1), NETWORK VARCHAR(25) NOT NULL, URL VARCHAR(256) NOT NULL,PRIMARY KEY (ID))
webjauth=CREATE TABLE APP.WEBJAUTH (ID INTEGER NOT NULL GENERATED ALWAYS AS IDENTITY (START WITH 1, INCREMENT BY 1), MYAUTH VARCHAR(256) NOT NULL, SECRET VARCHAR(256) NOT NULL,PRIMARY KEY (ID))